<h2>Update the drone</h2>
<div>
    <form method="POST" action="/drones/{{drone._id}}/edit">
        <div class="form">
            <label>Name</label>
            <input type="text" value="{{drone.name}}">
        </div>

        <div class="form">
            <label>Propellers</label>
            <input type="text" value="{{drone.propellers}}">
        </div>

        <div class="form">
            <label>Max Speed</label>
            <input type="text" value="{{drone.maxSpeed}}">
        </div>

        <button type="submit">Update</button>

    </form>
</div>